*.iml
.gradle/
.gradle-home/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
app/build/

# Internal PRD / design doc — not for the public repo
/Reconstruct Phoenix then compare with PRD file.docx

# Obsidian vault config (the specs/ folder doubles as an Obsidian vault).
# Local workspace/theme state — not versioned. Delete this line to share vault settings.
.obsidian/

# Graphify tooling: locally-provisioned CLI + Kotlin grammar (see scripts/graphify/).
# Regenerated on demand; never committed. The graph itself lives in .graphify/.
.graphify-tools/
